import React from "react";

const HelloWorld = () => {
    return (
        <div>
            <h1>Hello World Radha aman  </h1>
        </div>
    );
};

export default HelloWorld; // ✅ Normal React export, no `hot()`
